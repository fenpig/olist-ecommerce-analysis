# 遗留 SQL 资产

本目录文件来自旧版 Olist 项目，保留用于追溯和参考。

- 它们使用历史数据库名称 `olist_analysis`。
- 它们不属于一期正式 SQL 执行顺序。
- 不应在 `olist_delivery_analysis` 中直接执行。
- 文件内容在 T06 隔离时未被修订；移动前后的 SHA-256 见 `docs/LEGACY_MIGRATION_MANIFEST.md`。

一期正式 SQL 位于仓库根目录 `sql/`，并以 `docs/SQL_EXECUTION_ORDER.md` 为唯一执行顺序说明。
